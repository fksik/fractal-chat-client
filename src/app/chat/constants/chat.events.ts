export enum ChatEvents {
  SEND_MESSAGE = 'server-incoming',
  RECEIVE_MESSAGE = 'server-outgoing',
  USER_TYPING = 'user-typing',
  USER_REGISTERED = 'user-registered',
  CONVERSATION_CREATED = 'conversation-created'
}
